export interface Project {
    title: string;
    description: string;
    technologies: string[];
    githubLink:string; //remove this later
    demoLink: string;
    image: string;
}

export interface Experience{}